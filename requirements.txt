# Requirements for Brick E1 – Cloud Object Storage

# Core filesystem & S3 support
fsspec>=2023.1.0
s3fs>=2023.1.0

# Raster processing & COG creation
rasterio>=1.3.0
rio-cogeo>=3.4.0

# Vector processing & GeoParquet
geopandas>=0.13.0
pyarrow>=11.0.0
python-geohash>=0.8.5  # Updated to match available PyPI version

# (Optional) If running tests or additional utilities
# pandas>=1.5.0
# pytest>=7.0.0